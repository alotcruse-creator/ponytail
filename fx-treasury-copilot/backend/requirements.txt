fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
# Both AI providers are optional: set either key and the other is a fallback.
# With no key set, the app returns deterministic summaries and needs neither.
anthropic==0.39.0
openai==1.51.0
